package strategy.ex1;

/**
 * This Strategy interface is implemented by all concrete objects representing
 * an algorithm(strategy), which lets us define a family of algorithms.
 */
public interface GoAlgorithm {
	public void go();
}
